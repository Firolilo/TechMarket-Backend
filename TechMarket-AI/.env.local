# Local secrets — NOT committed (see .gitignore). Rotate keys freely in AI Studio.
# Google AI Studio (Gemini) free-tier key, used by the "gemini" Spring profile.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview